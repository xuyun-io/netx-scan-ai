services:
  netx-sre-agent:
    build:
      context: .
      dockerfile: Dockerfile
    image: netx-sre-agent:latest
    container_name: netx-sre-agent
    ports:
      - "8080:8080"
    volumes:
      - ./agent-server/data/agents:/data/agents
      - ./agent-server/skills:/app/skills:ro
